# Utilities

- `motion/`: motion recovery, feature extraction, rotations, skeletons, and filtering.
- `training/`: losses, checkpoint loading, and sampling wrappers.
- `runtime/`: arguments, distributed execution, seeds, and paths.
